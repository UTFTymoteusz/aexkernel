
    
    /*process_create("system2", "/sys/aexkrnl.exe", 0);
    process_create("system3", "/sys/aexkrnl.a.out", 0);

    klist_entry_t* entry = NULL;
    void* ptr;
    struct process* proc;

    while (true) {
        ptr = klist_iter(&process_klist, &entry);

        if (ptr == NULL)
            return;

        proc = (struct process*)ptr;

        write_debug("Boi: %s\n", proc->pid, 10);
    }*/